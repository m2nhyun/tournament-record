# UX Tasks (Club & Match)

## Done

### 1) 클럽명 변경 (클럽장 전용)
- 완료: owner 전용 UI + RPC 반영

### 2) 클럽명/닉네임 중복 방지
- 완료: 정규화 unique 제약 + 사용자 메시지 매핑

### 3) 내 클럽 닉네임/개인 설정
- 완료: 멤버 목록의 내 항목 모달에서 변경

### 4) 히스토리 뷰 개선
- 완료: 카드/리스트 모드, 필터, 무한 스크롤
- 완료: 리스트 모드 최소형(`팀A x:y 팀B`) + 배경색 승패 표시

### 5) 초대 플로우 개선
- 완료: 원클릭 초대 링크(`/join/[inviteCode]`)
- 완료: 링크 복사/카카오 공유
- 완료: 초대 만료일 + owner 재발급

### 6) 게스트 정책
- 완료: 게스트 참가 허용
- 완료: 게스트 경기 생성/수정 금지

### 7) 멤버 삭제 정책
- 완료: owner 멤버 제외(소프트 삭제) + 기록 보존

### 8) 경기 확인 플로우
- 완료: 경기 저장 직후 `submitted` 상태 유지
- 완료: 상대 팀 확인 대상 승인/거절 액션 도입
- 완료: 전원 승인 시 `confirmed`, 거절 시 `disputed` 전환
- 완료: 리더보드는 `confirmed` 경기만 반영

### 9) 일정 모집 베이스
- 완료: `새 경기` 화면에 `경기 기록 / 일정 잡기` 분기 추가
- 완료: 날짜/시간/장소/코트비/캔볼/모집 인원/복식 타입으로 일정 생성 가능
- 완료: 개설자 자동 참가 + 남은 자리 계산 + 클럽 홈 upcoming 일정 목록 반영

## Doing

### 1) UI 일관성 강화
- AppBar + content(px-4) 구조를 전 화면에 지속 적용
- 컴포넌트별 간격/타이포 편차 정리

### 2) Auth / 온보딩 설계 고정
- 이메일/카카오 병행 정회원 구조와 프로필 완료 가드를 설계 문서로 고정
- 상세 설계: `docs/11-auth-onboarding-design.md`
- 상태: 1차 연결 완료
- 반영 내용: `/auth/callback` 분기, `/onboarding/profile`, 홈 진입 유도, 클럽/일정/경기 저장의 서비스 가드

### 3) 홈 대시보드의 클럽 활성화 방향 정리
- 홈을 단순 진입점이 아니라 `클럽 최근 활동` 중심 대시보드로 강화
- 운영진/멤버/게스트별 첫 화면 우선순위 정리

### 4) 커뮤니티 / 매칭 시스템 방향 정리
- 큰 카테고리는 `커뮤니티`, `클럽 내 일정 기반 매칭 시스템` 2축으로 고정
- 현재 구현된 일정 생성/참가 루프를 기반으로, 이후 확장은 `일정 상세 -> 참가 맥락 강화 -> 실제 경기 연결` 순서로 진행
- 채팅/댓글 같은 커뮤니티 기능은 독립 도메인으로 급하게 분리하기보다, 우선 `일정` 엔티티를 중심으로 대화와 참가 컨텍스트를 쌓는 방향을 우선 검토

## Backlog (P0)

### 1) 클럽 도입 퍼널 단순화
- 목표: 클럽 생성 -> 초대 링크 공유 -> 첫 경기 기록까지의 마찰 최소화
- 검토 범위: 홈 CTA 구조, 초대 링크 카피, 게스트 후 등록 전환 시점

### 2) 정모 직후 기록 UX 압축
- 목표: 운영진이 30초 안팎으로 경기 기록을 끝낼 수 있게 단계를 더 줄인다
- 검토 범위: 선수 선택, 점수 선택, 저장 후 확인 요청 카피

### 3) 미확정 경기 후속 처리 강화
- 목표: `기록됨/재검토 필요` 경기가 방치되지 않도록 확인 액션 진입성을 높인다
- 검토 범위: 홈 알림, 히스토리 강조, 경기 상세 CTA

### 4) 일정 상세 진입점 추가
- 목표: 클럽 홈의 일정 카드에서 상세 화면으로 들어가 참가자, 비용, 메모, 남은 자리, 후속 액션을 한 화면에서 본다
- 검토 범위: 일정 상세 라우트, 일정 카드 CTA, 호스트/참가자/게스트별 액션 분기
- 상태: 1차 구현 완료
- 반영 내용: 클럽 홈 일정 카드에 `상세 보기`를 추가하고, 일정 상세 라우트에서 참가 현황과 참가/취소 액션을 제공

### 5) 일정 기반 참가 시스템 고도화
- 목표: `누군가 일정을 올리면 참가할 수 있는 시스템`을 단순 버튼 수준에서 `상태가 보이는 모집 경험`으로 끌어올린다
- 검토 범위: 정원 마감 상태 표현, 참가자 목록 가독성, 참가/취소 피드백, 개설자 고정 표시, 일정 상태 카피
- 상태: 1차 구현 완료
- 반영 내용: `바로 참가`와 `승인 후 참가`를 일정 생성 단계에서 고를 수 있고, 일정 상세/클럽 홈 카드에서 `참가 신청`, `신청 취소`, `대기 신청 건수`, `승인 대기(reviewing)` 상태를 함께 노출

### 6) 호스트 관리 액션
- 목표: 일정 개설자가 일정 상태를 운영할 수 있게 한다
- 검토 범위: 일정 취소, 모집 마감, 필요 시 일정 수정, 참가자 확정 흐름
- 상태: 부분 구현
- 반영 내용: 승인형 모집의 신청 수락/거절은 일정 상세에서 처리 가능
- 남은 범위: 일정 취소, 모집 마감, 일정 수정, 실제 경기 연결

### 7) 일정에서 실제 경기로 연결
- 목표: 일정이 끝난 뒤 실제 경기 기록으로 자연스럽게 이어진다
- 검토 범위: `linked_match_id` 활용, 일정 상세의 `경기 기록 시작` CTA, 일정 참가자 기반 선수 선택 초기값

### 8) 정회원 프로필 온보딩
- 목표: 이메일/카카오 가입 후 닉네임/성별을 수집하고, 프로필 완료 후에만 핵심 쓰기 기능을 허용한다
- 검토 범위: `user_profiles` 전역 프로필, `/onboarding/profile`, `/auth/check-email`, 이메일 인증 UX, `profile_completed` 기반 가드
- 설계 문서: `docs/11-auth-onboarding-design.md`

## Backlog (P1)

### 1) 상대 검색 정밀화
- 목표: 내 상대 기준 검색 정확도 향상

### 2) 홈 대시보드 재구성
- 목표: `내 최근 경기`, `클럽 최근 경기`, `확인 대기`, `리더보드 요약`을 한 화면에서 본다

### 3) 변경 이력 가시화
- 목표: 클럽 설정/멤버 변경 타임라인 UI

### 4) 테스트 보강
- 목표: 권한/게스트/초대 만료/경기 확인 회귀 테스트 강화

### 5) 일정 후속 액션
- 목표: 일정 상세, 취소/마감, 채팅방 연결, 실제 경기 기록 연결까지 확장
- 검토 범위: 일정 상세 라우트, 개설자 관리 액션, 채팅방 엔티티, 일정 -> 경기 기록 전환 UX

### 6) 커뮤니티 레이어 1차
- 목표: 일정을 중심으로 멤버 간 상호작용을 붙여 `참가 모집 게시판`처럼 느껴지게 한다
- 검토 범위: 일정별 공지/메모 강화, 참가 의도 표현, 호스트 공지, 추후 댓글/채팅 연결 기준

### 7) 클럽 내 매칭 레이어 1차
- 목표: 일정이 단순 모집을 넘어서 “누가 함께 칠지”가 보이는 매칭 준비 단계로 진화한다
- 검토 범위: 참가자 역할/선호 표시, 복식 조합 준비 정보, 일정 종료 후 경기 기록 연결

## Backlog (P2)

### 1) 캘린더 기반 탐색
- 날짜 중심 경기 조회와 일정 탐색 경험 확장

### 2) 통계 고도화
- 상대전적, 파트너 조합, 기간별 지표

### 3) 운영진용 활동 요약
- 주간/월간 활동량, 기록 누락 점검, 자주 뛰는 멤버 가시화

## Recent Updates

### 2026-03-10 경기 확인 UX 정리 완료
- `submitted`는 `기록됨`, `disputed`는 `재검토 필요` 중심 카피로 정리
- 경기 상세에서 확인 대상 닉네임과 현재 확인 상태를 함께 안내
- `disputed` 경기 상세와 수정 화면에서 `수정 후 다시 저장 -> 새 확인 요청 전송` 흐름을 명시
- 재검토 상태 경기 수정 성공 메시지를 재확인 요청 중심으로 유지

### 2026-03-11 최적화/UI·UX 보강
- 클럽 상세 mutation 후 전체 재조회 대신 로컬 상태 갱신으로 반응성을 개선
- 클럽 상세에 `코드 복사`, `링크 복사`, `링크 공유`, `재발급(owner)` 액션을 역할별로 정리
- `/join/[inviteCode]` 라우트를 임시 비활성화 카드에서 실제 `InviteJoinView`로 다시 연결
- 홈/초대 참가/email 인증 버튼을 모바일 우선 배치로 조정하고 게스트/정회원 분기를 더 명시
- 히스토리 목록 remount를 제거하고 상대 검색을 `내 상대` 기준으로 정밀화

### 2026-03-13 PM / MVP 방향 정리
- 제품을 `개인 기록 앱`보다 `클럽 운영용 경기 기록 플랫폼`으로 재정의
- 활성화 루프를 `정모 기록`, `클럽 내 경쟁`, `운영 편의` 3축으로 정리
- UX 백로그 우선순위를 `도입 퍼널`, `기록 속도`, `미확정 후속 처리` 중심으로 재배치

### 2026-03-17 일정 모집 베이스 추가
- `새 경기`에서 `경기 기록`과 `일정 잡기`를 분리해 사전 모집과 사후 기록을 같은 진입점에서 처리
- 클럽 홈에 upcoming 일정 리스트를 추가하고, 참가/취소 액션과 남은 자리 수를 바로 노출
- 채팅방/실제 경기 연결은 일정 엔티티를 기준으로 후속 확장하도록 정리
- 일정 생성 화면은 기본 브라우저 입력 대신 인라인 캘린더와 클릭형 시간 선택 UI로 보강

### 2026-03-17 일정 생성 UX 보강
- 다중 선택처럼 보이지만 실제 저장은 1건만 되는 혼란을 제거하고, `한 번에 하나의 일정 생성` 흐름으로 정리
- `내일/이번 토/이번 일` 빠른 날짜 버튼을 추가해 운영진의 현장 입력 속도를 높임
- 장소, 정원, 예상 비용을 생성 전 요약 카드에서 다시 보여줘 저장 전 검토 비용을 줄임
- 입력값을 수정하면 기존 에러를 즉시 지워 재시도 흐름이 끊기지 않게 조정

### 2026-03-19 커뮤니티 / 매칭 방향 고정
- 이후 큰 카테고리를 `커뮤니티`, `클럽 내 일정 기반 매칭 시스템`으로 정리
- 현재 일정 생성/참가의 최소 루프는 이미 구현된 상태로 보고, 다음 우선순위를 `일정 상세`, `호스트 관리`, `일정 -> 경기 연결` 쪽으로 상향
- 커뮤니티 기능은 일정 엔티티를 중심으로 붙이고, 채팅/댓글은 일정 상세와 참가 맥락이 정리된 뒤 확장하는 방향으로 정리

### 2026-03-19 일정 상세 1차 추가
- 클럽 홈 upcoming 일정 카드에 `상세 보기` CTA를 추가
- 일정 상세 라우트(`/clubs/[clubId]/schedules/[scheduleId]`)를 만들어 참가자, 비용, 메모, 남은 자리, 참가/취소 액션을 한 화면에서 보게 정리
- 호스트 관리 액션과 일정 -> 실제 경기 연결은 다음 단계 확장 대상으로 유지

### 2026-03-19 일정 생성 디테일 개선
- 캘린더의 현재 선택 날짜 강조를 더 분명하게 보이도록 정리
- 시간 선택을 단일 시각이 아니라 연속 슬롯 선택 기반 범위(`12:00`, `13:00` -> `12:00 ~ 14:00`)로 확장
- 코트비, 캔볼 가격, `본인 포함`을 체크 기반으로 바꾸고 기본값은 모두 체크 상태로 유지
- `본인 포함`을 끄면 개설자는 참가자에 자동 포함되지 않고, 입력한 정원 전체를 모집 인원으로 사용하도록 정리
- 일정 카드/상세에 `개설자 미포함` 상태를 드러내고, 인당 예상 비용을 더 명확하게 노출
