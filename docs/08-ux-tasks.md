# UX Tasks (Club & Match)

## Done

### 1) 클럽명 변경 (클럽장 전용)
- 완료: owner 전용 UI + RPC 반영

### 2) 클럽명/닉네임 중복 방지
- 완료: 정규화 unique 제약 + 사용자 메시지 매핑

### 3) 내 클럽 닉네임/개인 설정
- 완료: 멤버 목록의 내 항목 모달에서 변경

### 4) 히스토리 뷰 개선
- 완료: 카드/리스트 모드, 필터, 무한 스크롤
- 완료: 리스트 모드 최소형(`팀A x:y 팀B`) + 배경색 승패 표시

### 5) 초대 플로우 개선
- 완료: 원클릭 초대 링크(`/join/[inviteCode]`)
- 완료: 링크 복사/카카오 공유
- 완료: 초대 만료일 + owner 재발급

### 6) 게스트 정책
- 완료: 게스트 참가 허용
- 완료: 게스트 경기 생성/수정 금지

### 7) 멤버 삭제 정책
- 완료: owner 멤버 제외(소프트 삭제) + 기록 보존

### 8) 경기 확인 플로우
- 완료: 경기 저장 직후 `submitted` 상태 유지
- 완료: 상대 팀 확인 대상 승인/거절 액션 도입
- 완료: 전원 승인 시 `confirmed`, 거절 시 `disputed` 전환
- 완료: 리더보드는 `confirmed` 경기만 반영

### 9) 일정 모집 베이스
- 완료: `새 경기` 화면에 `경기 기록 / 일정 잡기` 분기 추가
- 완료: 날짜/시간/장소/코트비/캔볼/모집 인원/복식 타입으로 일정 생성 가능
- 완료: 개설자 자동 참가 + 남은 자리 계산 + 클럽 홈 upcoming 일정 목록 반영

## Doing

### 1) UI 일관성 강화
- AppBar + content(px-4) 구조를 전 화면에 지속 적용
- 컴포넌트별 간격/타이포 편차 정리

### 2) 홈 대시보드의 클럽 활성화 방향 정리
- 홈을 단순 진입점이 아니라 `클럽 최근 활동` 중심 대시보드로 강화
- 운영진/멤버/게스트별 첫 화면 우선순위 정리

## Backlog (P0)

### 1) 클럽 도입 퍼널 단순화
- 목표: 클럽 생성 -> 초대 링크 공유 -> 첫 경기 기록까지의 마찰 최소화
- 검토 범위: 홈 CTA 구조, 초대 링크 카피, 게스트 후 등록 전환 시점

### 2) 정모 직후 기록 UX 압축
- 목표: 운영진이 30초 안팎으로 경기 기록을 끝낼 수 있게 단계를 더 줄인다
- 검토 범위: 선수 선택, 점수 선택, 저장 후 확인 요청 카피

### 3) 미확정 경기 후속 처리 강화
- 목표: `기록됨/재검토 필요` 경기가 방치되지 않도록 확인 액션 진입성을 높인다
- 검토 범위: 홈 알림, 히스토리 강조, 경기 상세 CTA

## Backlog (P1)

### 1) 상대 검색 정밀화
- 목표: 내 상대 기준 검색 정확도 향상

### 2) 홈 대시보드 재구성
- 목표: `내 최근 경기`, `클럽 최근 경기`, `확인 대기`, `리더보드 요약`을 한 화면에서 본다

### 3) 변경 이력 가시화
- 목표: 클럽 설정/멤버 변경 타임라인 UI

### 4) 테스트 보강
- 목표: 권한/게스트/초대 만료/경기 확인 회귀 테스트 강화

### 5) 일정 후속 액션
- 목표: 일정 상세, 취소/마감, 채팅방 연결, 실제 경기 기록 연결까지 확장
- 검토 범위: 일정 상세 라우트, 개설자 관리 액션, 채팅방 엔티티, 일정 -> 경기 기록 전환 UX

## Backlog (P2)

### 1) 캘린더 기반 탐색
- 날짜 중심 경기 조회와 일정 탐색 경험 확장

### 2) 통계 고도화
- 상대전적, 파트너 조합, 기간별 지표

### 3) 운영진용 활동 요약
- 주간/월간 활동량, 기록 누락 점검, 자주 뛰는 멤버 가시화

## Recent Updates

### 2026-03-10 경기 확인 UX 정리 완료
- `submitted`는 `기록됨`, `disputed`는 `재검토 필요` 중심 카피로 정리
- 경기 상세에서 확인 대상 닉네임과 현재 확인 상태를 함께 안내
- `disputed` 경기 상세와 수정 화면에서 `수정 후 다시 저장 -> 새 확인 요청 전송` 흐름을 명시
- 재검토 상태 경기 수정 성공 메시지를 재확인 요청 중심으로 유지

### 2026-03-11 최적화/UI·UX 보강
- 클럽 상세 mutation 후 전체 재조회 대신 로컬 상태 갱신으로 반응성을 개선
- 클럽 상세에 `코드 복사`, `링크 복사`, `링크 공유`, `재발급(owner)` 액션을 역할별로 정리
- `/join/[inviteCode]` 라우트를 임시 비활성화 카드에서 실제 `InviteJoinView`로 다시 연결
- 홈/초대 참가/email 인증 버튼을 모바일 우선 배치로 조정하고 게스트/정회원 분기를 더 명시
- 히스토리 목록 remount를 제거하고 상대 검색을 `내 상대` 기준으로 정밀화

### 2026-03-13 PM / MVP 방향 정리
- 제품을 `개인 기록 앱`보다 `클럽 운영용 경기 기록 플랫폼`으로 재정의
- 활성화 루프를 `정모 기록`, `클럽 내 경쟁`, `운영 편의` 3축으로 정리
- UX 백로그 우선순위를 `도입 퍼널`, `기록 속도`, `미확정 후속 처리` 중심으로 재배치

### 2026-03-17 일정 모집 베이스 추가
- `새 경기`에서 `경기 기록`과 `일정 잡기`를 분리해 사전 모집과 사후 기록을 같은 진입점에서 처리
- 클럽 홈에 upcoming 일정 리스트를 추가하고, 참가/취소 액션과 남은 자리 수를 바로 노출
- 채팅방/실제 경기 연결은 일정 엔티티를 기준으로 후속 확장하도록 정리
- 일정 생성 화면은 기본 브라우저 입력 대신 인라인 캘린더와 클릭형 시간 선택 UI로 보강
